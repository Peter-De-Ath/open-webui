services:
  open-webui:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: /bin/bash -c "cd /app && npm install && npm run dev & cd /app/backend && ./dev.sh"
    volumes:
      - ./.svelte-kit:/app/.svelte-kit
      - ./backend:/app/backend
      - ./backend/data:/app/backend/data # Overriding the docker volume with a local directory
      - ./components:/app/components
      - ./frontend:/app/frontend
      - ./node_modules:/app/node_modules
      - ./scripts:/app/scripts
      - ./src:/app/src
      - ./static:/app/static
      - ./styles:/app/styles
      - ./test:/app/test
      - ./utils:/app/utils
      - ./.env:/app/.env
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json
      - ./postcss.config.js:/app/postcss.config.js
      - ./svelte.config.js:/app/svelte.config.js
      - ./tailwind.config.js:/app/tailwind.config.js
      - ./tsconfig.json:/app/tsconfig.json
      - ./vite.config.ts:/app/vite.config.ts
    environment:
      # Development environment settings
      - ENV=dev
      - NODE_ENV=development
      # Enable source maps for better debugging
      - VITE_ENABLE_SOURCEMAPS=true
      - VITE_HOST=0.0.0.0
      - 'WEBUI_SECRET_KEY='
    ports:
      - '8080:8080'
      - '5173:5173'
